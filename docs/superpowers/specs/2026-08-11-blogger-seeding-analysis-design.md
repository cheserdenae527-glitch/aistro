# 博主种草能力分析系统重构 — 设计 v1

> 状态：设计稿，尚未改代码。
> 目标场景：用账号数据回答「这个餐饮博主种草能力强不强、值不值得合作」。
> 关联：`docs/DESIGN-BLOGGER-SCORING-REALDATA.md`（C9，评分部分被本设计取代）/ `docs/SPEC-CRAWLER.md` §11。
> 决策记录（2026-08-11 与用户确认）：
> - 使用场景 = C：同一套评分模型，同时支撑「单号深度诊断」与「批量筛选排名」
> - 品类 = A：餐饮/美食专属（种草能力 = 能否给餐饮商家带客）
> - 评论 = C：可选开关，默认关闭，深度诊断可手动开启
> - 方案 = A：种草导向五维重构（保留 C9 真实数据骨架）
> - 旧引擎 = A：`xhs_analysis.py` 与同步分析接口直接下线

---

## 0. 设计目标

分数只回答一个问题：**这个餐饮博主的种草能力强不强，值不值得合作**。

- 种草能力 = 内容让人「想去吃 / 想安利」的能力：收藏（想去）、评论意向（想吃）、分享（安利）、转粉（关注）。
- 低质号识别独立于分数，用资格闸门一票否决（可不信的账号不给分）。
- 同一模型两种消费：单号深度报告看解释力，批量筛选看排序与硬门槛。

## 1. 行业调研结论（对比分析摘要）

调研来源：千瓜数据（达人指数/笔记传播指数/KOC 价值模型）、新榜（黄金三角）、天擎天拓（5 维雷达图/假量特征库）、Hashmeta（五维模型）、小红书 CES 公式与官方「真诚分」体系、2025 行业涨粉报告、多篇运营实操文章。

### 1.1 主流评估体系对比

| 体系 | 维度 / 权重 | 核心逻辑 |
|---|---|---|
| 千瓜达人指数 | 创作能力 50% + 粉丝活跃度 30% + 达人活跃度 20% | 创作能力 = 笔记互动 vs 同粉丝量级达人均值（分层归一化的行业依据） |
| 千瓜笔记传播指数 | (赞/均值)×1 + (藏/均值)×1 + (评/均值)×4 | 评论权重显著高于赞藏 |
| 千瓜 KOC 价值模型 | 超额互动(CPE<1) + 种草效果 + 转化效果 | 垂直度、评论区正面/产品词、@好友与分享异常 |
| 新榜黄金三角 | 互动率 + 涨粉率 + 完播率 | 互动率(赞评藏/粉丝)>8%、近30天涨粉率>3%、完播率>40% → 推荐概率高 2.3 倍 |
| 天擎天拓 5 维雷达图 | 互动深度 + 粉丝质量 + 内容垂直度 + 商业口碑 + 人设清晰度 | **收藏量才是种草力核心信号**；警惕高赞低藏/泛化评论刷量号 |
| Hashmeta 五维 | 互动率 25% + 粉丝量与增长 20% + 内容质量 20% + 画像匹配 20% + 商业配合 15% | 互动率+画像匹配合计 45% |
| 小红书 CES | 赞×1 + 藏×1 + 评×4 + 转×4 + 关注×8 | 关注(转粉)权重最高 |
| 官方 | 真诚分 / 健康等级 / 内容质量分权重 40% | 平台自己按账号分级扶优抑差 |

**共性结论**：没有体系把粉丝量当主权重；权重共识为「互动深度 > 内容稳定与垂直 > 涨粉 > 活跃度」，粉丝量只做分层基准。

### 1.2 关键指标基准（结构占位，待餐饮样本标定）

| 指标 | 行业基准 |
|---|---|
| 互动率(赞藏评/粉丝) | 1–3% 标准 / 3–8% 健康 / 8–12% 优秀 / 12%+ 顶尖 |
| 收藏率 | 3–5% 为优秀（收藏/阅读口径；我们无阅读量，改用 收藏/粉丝/篇） |
| 爆文率 | 10–20% 合格；优质账号 >20%（无阅读量，用互动量超均值 3 倍口径） |
| 赞藏比(藏/赞) | 干货/探店 收藏>点赞；赞:藏≈2:1 正常，藏/赞<0.2 为异常信号 |
| 赞藏粉比 | 3–10 正常，>10 有爆文但转粉差 |
| 涨粉率(月) | >3% 健康（新榜）；分量级：<5w≈6%、5w–50w≈9%、50w–300w≈8%、300w–1000w≈10%、>1000w≈4% |
| 更新频率 | 每周 2–3 篇健康；<2 篇/周持续 3 个月 = 合作风险 +40% |

### 1.3 低质号 / 刷量特征库

| 类别 | 信号 |
|---|---|
| 刷量互动 | 互动率长期 <2%；赞藏比倒挂（高赞低藏）；通用水评；数据篇篇雷同；粉丝少但点赞异常高 |
| 粉丝注水 | 粉丝走势剧烈起伏/集中涌入后零互动；粉赞比失真；头像模糊/主页空白/无意义昵称 |
| 营销号 | 商单占比过高、评论区「广告取关」频繁、内容跨领域杂乱、更新机构化 |

## 2. 评分模型总览

```
种草能力总分 (0-100) = 五维加权
   ├─ 种草深度 30%   （收藏=想去、赞藏结构、分享=安利、评论信号可选）
   ├─ 内容垂直度 20% （美食/探店内容占比）
   ├─ 稳定产出 20%   （爆文率 + 波动 CV）
   ├─ 持续经营 15%   （更新频率 + 新鲜度）
   └─ 增长趋势 15%   （涨粉率 + 内容互动趋势 + 账号阶段）

叠加：资格闸门（一票否决，优先于分数）→ 账号阶段标签 → 合作建议
```

## 3. 数据准入与分层（沿用 C9 骨架）

- **真实样本**：详情抓取成功 + 赞/藏/评/转四字段齐全 + 有明确发布时间。缺失就是缺失，绝不估算、不外推。
- **粉丝分层**：T1 1k–1w（素人/KOC）、T2 1w–10w（初级/腰部）、T3 10w–100w（腰部/头部）、T4 ≥100w（头部/顶流）；互动率、收藏率、涨粉率的映射基准按层分别标定。
- **类型内标准化**：图文/视频按类型中位数归一，防混发账号被误判（探店类以图文为主）。
- **覆盖率可信度**（沿用 C9）：≥80% 且 ≥30 篇 = 高；≥50% 且 ≥15 篇 = 中；低于 = 低（不评分）。

## 4. 五维评分细则

### 4.1 种草深度 — 30%（新增核心）

回答「内容让多少人想去/想安利」，餐饮口径：

```
收藏深度分 = 近90天篇均收藏 / 粉丝数，按粉丝分层映射（收藏 =「想去吃」最强信号）
赞藏结构分 = 真实样本 收藏/点赞 中位数：≥1.0→100，0.5→80，0.3→60，<0.2→30
分享扩散分 = 近90天篇均分享 / 粉丝数，按分层映射（最难伪造）
评论信号分 = 评论参与度 = 评论数 / (赞+藏+评+转)；开启评论分析后升级为 意向词占比 − 水评占比（见 §9）

种草深度 = 收藏深度×0.45 + 赞藏结构×0.20 + 分享扩散×0.15 + 评论信号×0.20
```

**边界**：收藏/分享中位数为 0 时该项按 0 计，由闸门兜底（刷量/倒挂）。

### 4.2 内容垂直度 — 20%（新增）

回答「是不是真正的美食/探店博主」：

```
每篇笔记用 标题+正文+标签 关键词判定是否餐饮相关
（探店/打卡/美食/好吃/菜单/套餐/口味/推荐/人气/排队/新店 等，关键词表入配置）
垂直度 = 餐饮相关笔记数 / 真实样本数
映射：≥80%→100，60%→70，40%→40，<20%→10
```

- 无法判定（无标题/正文/标签）的笔记不计入垂直度计算；可判定笔记数 < 真实样本数×80% 时，该维度置信度降为低。
- 升级路径：Phase 2 可换大模型分类（关键词表为 V1 实现，成本最低）。

### 4.3 稳定产出 — 20%（沿用 C9）

```
爆文 = 加权互动 ≥ 账号均值 × 3（无阅读量，用互动量口径替代行业「阅读超均值3倍」）
优质笔记占比 = 爆文数 / 真实样本数（行业合格线 10–20%）
产出波动 CV = 真实样本标准化互动的变异系数
稳定产出 = 优质笔记占比分×0.7 + (1−min(CV,1))×100×0.3
```

**边界**：标准化互动全为 0 时，CV 无意义，波动项置中性 50。

### 4.4 持续经营 — 15%（沿用 C9）

```
发布频率分（近90天周均）：≥3→100，2→75，1→50，0.5→30，<0.5→0
新鲜度分（最新真实笔记发布距今）：≤7天→100，≤30→70，≤60→40，>60→0
持续经营 = 发布频率分×0.6 + 新鲜度分×0.4
```

`>60 天` 同时触发 §6 发布停滞闸门（维度降分 + 等级再降档）。

### 4.5 增长趋势 — 15%（原「趋势」升级）

```
涨粉率 = 近30天粉丝增长率（需 ≥2 个粉丝快照：订阅快照 + justoneapi 均可）
  分层基准（结构占位，行业参考月均）：T1≈6%，T2≈9%，T3≈8%，T4≈7%
  涨粉分映射：≥基准×1.2→100，基准→75，0.5×基准→45，≤0→15
内容趋势 = 近半程 / 前半程平均标准化互动（沿用原趋势逻辑，样本<10 跳过并归一权重）

增长趋势 = 涨粉分×0.7 + 内容趋势分×0.3
```

**边界**：无涨粉快照时，涨粉子项置「未知」，改用 增长趋势 = 内容趋势分×0.7 + 阶段分×0.3（阶段分：成熟/成长=100，冷启动=60，衰退=30），并标注低置信；不编造。

## 5. 账号阶段判定（独立输出标签）

| 阶段 | 判定信号 |
|---|---|
| 冷启动 | 粉丝 <5k，有更新但互动低、涨粉数据不足 |
| 成长 | 涨粉率 ≥ 分层基准，互动率爬升 |
| 成熟 | 涨粉放缓但互动稳定、内容垂直（最值得合作） |
| 衰退 | 涨粉率 ≤0 或显著低于基准 + 更新频率下降 + 互动下滑 |

- 有涨粉快照：按 涨粉率 vs 基准 + 更新频率 判定。
- 无快照：按 粉丝量级 + 更新频率 + 互动趋势（近30天 vs 前60天）推断，标注置信度。
- 阶段不参与总分加权，作为独立标签输出，并给「增长趋势」维度提供阶段分参考。

## 6. 低质号闸门（一票否决，优先于分数）

| 优先级 | 闸门 | 判定 | 处理 |
|---|---|---|---|
| 1 | 数据覆盖率不达标 | §3 低可信度 | 返回「数据不足，暂不评分」 |
| 2 | 刷量嫌疑 | C9 规则（点赞≥中位数×3 且 藏评转/赞<0.5%，命中>20%）+ **新增：赞藏比中位数<0.2 且 篇均收藏/粉丝 低于该层最低健康线** + （可选）水评占比超阈值 | 强制「不合作」 |
| 3 | 粉丝互动倒挂 | 篇均互动率低于该层最低健康线 | 等级封顶「待观察」 |
| 4 | 发布停滞 | 最新真实笔记发布距今 >60 天 | 等级降一档 |
| 5 | 涨粉异常 | 相邻快照增幅 >20%，或突增后互动率下降（僵尸粉信号） | 提示级红旗（不改等级） |

命中闸门 2 时覆盖加权分数，直接输出强制结论。闸门结果写入结果 JSON 的 `red_flags`，作为「合作建议」与「批量筛选」的依据。

## 7. 合作决策输出

**等级**（沿用）：≥85 卓越 / 70 优秀 / 55 良好 / 40 一般 / <40 待观察。

**合作建议 4 档**（总分 + 阶段 + 红旗共同决定）：

| 建议 | 条件 |
|---|---|
| 优先合作 | 总分 ≥70 且无红旗 且 阶段∈{成长, 成熟} |
| 可合作 | 总分 ≥55 且无强制红旗 |
| 谨慎 | 总分 40–54，或存在非致命红旗（倒挂/停更/涨粉异常） |
| 不合作 | 闸门命中（数据不足/疑似刷量） |

**输出结构**（一个引擎产出，两场景共用）：

```json
{
  "overall": { "score": 82.4, "level": "优秀", "description": "..." },
  "dimensions": {
    "seeding_depth": { "score": 88.0, "confidence": "high", "detail": { "collect_rate_percent": 1.9, "collect_like_ratio": 0.82, "share_rate_percent": 0.31, "comment_signal": 0.18 } },
    "verticality": { "score": 92.0, "confidence": "high", "detail": { "food_ratio": 0.92, "food_notes": 88, "total_notes": 96 } },
    "stable_output": { "score": 70.0, "confidence": "high", "detail": { "viral_ratio": 0.18, "cv": 0.45 } },
    "sustained_operation": { "score": 78.0, "confidence": "high", "detail": { "weekly_notes": 2.4, "freshness_days": 6 } },
    "growth_trend": { "score": 75.0, "confidence": "medium", "detail": { "growth_rate": 0.092, "trend_ratio": 1.3, "reason": null } }
  },
  "stage": { "label": "成长", "confidence": "medium", "evidence": ["近30天涨粉 +9.2%"] },
  "decision": {
    "recommendation": "优先合作",
    "summary": "美食垂直度高、收藏深度强，处于成长期，适合优先建联",
    "reasons": ["收藏深度超同层均值约 2 倍", "美食内容占比 92%", "近30天涨粉 +9.2%"],
    "red_flags": [ { "type": "stale", "level": "warn", "detail": "最新笔记发布距今 45 天" } ],
    "low_quality": false
  },
  "coverage": { "total_notes": 128, "fetched_notes": 96, "coverage_rate": 0.75, "sampled": true },
  "confidence": "medium",
  "grass_planting": null,
  "growth_potential": null,
  "notes": [],
  "timeline": [],
  "follower_history": {}
}
```

- `grass_planting` / `growth_potential` 旧字段：保留输出但由新五维/阶段替代语义（兼容前端），后续前端切新字段后移除。

## 8. 双场景支撑

### 8.1 单号深度诊断（升级现有 UserAnalysisPanel）
- 五维雷达图 + 总分/等级 + 阶段标签 + 合作建议卡片（结论 + 理由 + 红旗清单）
- 评论分析开关（§9）；开启时展示意向词/水评/负面信号
- 保留：覆盖率/可信度、真实笔记表格、周时间轴、涨粉历史曲线

### 8.2 批量筛选排名（新增列表视图）
- 硬门槛过滤：粉丝数下限、美食垂直度下限、更新频率下限、总分下限（配置）
- 排序：总分 desc → 可信度 → 篇均收藏
- 列表行：昵称/头像/阶段/总分/等级/建议/关键红旗/粉丝数/篇均收藏
- 流程：现有 `prescreen_user` 列表粗筛 → 通过者进后台深度评分任务 → 结果入列表（异步、进度可见）
- 复用 analysis-tasks API 与 CrawlerPoolPanel 等组件

## 9. 评论增强（可选开关，默认关）

- **触发**：单号深度诊断手动开启；批量筛选默认关闭
- **抓取**：5–10 篇代表性笔记（爆文 + 普通 + 最新）各抓 Top 50 条评论，复用现有评论爬虫与风控节奏（`analysis_batch_size` / `analysis_batch_interval_seconds`）
- **信号**：
  - 意向词占比（在哪/多少钱/好吃吗/怎么去/求地址/人均）→ 种草意向
  - 水评占比（太棒了/学习了/支持/求链接 等泛化词）→ 刷量嫌疑
  - 负面信号（广告/取关/踩雷/差评）→ 商业口碑
- **影响**：种草深度「评论信号」子项升级为意向词分析；刷量闸门增加「水评占比超阈值」判定
- 关键词表入配置，V1 用规则匹配，Phase 2 可升级语义模型

## 10. 配置与标定

- 权重、阈值、分层基准、餐饮关键词表、评论关键词表 → `crawler_config.json` 新增 `blogger_scoring` 段
- 所有具体百分比为**结构占位**，待真实餐饮样本标定（方法论沿用 C9 §8：每层 30–50 个餐饮账号，百分位反推 P10/P50/P90，交叉验证）
- config 预留 `weights` 段（未来 SaaS 化/目标切换扩展点），但本轮不做完整配置引擎（YAGNI）
- 分层阈值与互动率阈值随标定结果更新

## 11. 落地与兼容

- `backend/app/services/blogger_scoring.py`：重构为五维种草模型（保留真实数据准入/分层/标准化/闸门框架；新增垂直度/种草深度/阶段判定）
- `backend/app/services/analysis_task_runner.py`：新增评论可选阶段、结果写新字段
- **下线旧引擎**：
  - `backend/app/services/xhs_analysis.py` 删除（仅被同步分析接口与测试引用）
  - 同步接口 `POST /api/v1/notes/users/{user_id}/analysis` 下线（前端已全部走 `analysis-tasks`，无调用方）；同步删除 `_ANALYSIS_CACHE` 与 `UserAnalysisRequest`
  - `backend/tests/test_xhs_analysis.py` 删除；新增 `test_blogger_scoring` 覆盖五维/阶段/闸门
- 前端：升级 `UserAnalysisPanel.tsx`（五维 + 阶段 + 合作建议 + 评论开关）；`CrawlJobsPage.tsx` 新增批量筛选视图
- 结果 JSON 向后兼容：新增字段，旧字段保留过渡，前端切新字段后移除
- 设计文档与 SPEC-CRAWLER.md §11 同步更新

## 12. 明确不做（Phase 2 可选）

- 粉丝画像匹配（性别/年龄/地域/消费力：公开接口拿不到，需官方数据源或人工补充）
- 浏览量/完播率（公开 PC 接口不返回）
- 完整权重配置引擎（目标切换）
- 商单/报价/CPE 数据（需蒲公英/第三方）
- 评论内容质量深度语义分析（V1 用规则，Phase 2 可上模型）

## 13. 风险与开放问题

1. **阈值标定**：所有百分比为结构占位，需真实餐饮账号样本标定后才能作为生产数字；未标定前评分结果应标注「初版阈值」。
2. **涨粉快照依赖**：涨粉率与阶段判定依赖 ≥2 次快照；无快照账号该维度置信度低，需在 UI 明示。
3. **关键词表准确度**：美食垂直度 V1 依赖关键词表，可能漏判/误判（如探店文案不出现关键词）；Phase 2 用大模型分类兜底。
4. **评论抓取风控**：可选评论分析会放大请求量，需沿用 gate 节奏与失败降级（失败则回退到无评论信号，不影响主流程）。